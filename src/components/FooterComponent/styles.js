import { StyleSheet } from 'react-native';
const styles = StyleSheet.create({
  footerContainer:{
    paddingTop: 10,
    backgroundColor: '#FFF',
  },
  subText:{
    fontSize: 8
  }
});

export default styles;
