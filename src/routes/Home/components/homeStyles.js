import { StyleSheet } from 'react-native';
import { Colors, Layout } from '../../../constants/index';

const styles = {
  textAnnounce:{
    fontSize: 18,
    fontWeight: 'bold',
    marginTop: 10,
    marginLeft: 15,
    marginBottom: 10
  }
}

export default styles;
